# DIP 專案演算法開發與協作規範 (AGENTS.md)

本文件定義了使用者（開發者）與 AI 協同開發 `dip_proc.dll` 影像處理演算法的核心流程與規範。在接下來的開發過程中，**所有 AI 代理（Agents）均須嚴格遵守以下流程**。

---

## 0. 核心原則與開發限制 (Core Principles & Constraints)
1. **純文字對話輸出規範**：AI 代理在與使用者對話時，輸出應僅使用純文字，**嚴禁使用 LaTeX 格式（例如 $...$ 或 $$...$$ 格式的公式）以及其他可能無法在聊天介面或編輯器中正常顯示的特殊字元**。
2. **編譯器路徑規範**：在所有驗證、編譯或建置步驟中，**必須精確使用專案根目錄下 `.vscode/tasks.json` 中所定義的 MSBuild 執行檔路徑**（即 `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\amd64\MSBuild.exe`），嚴禁尋找或使用系統環境變數中其他的 MSBuild 執行檔。

---


## 1. 撰寫階段 (Writing & Development Stage)

此階段著重於單一演算法的觀念釐清與架構設計，嚴禁在未獲得授權前直接實作程式碼。

### 協作步驟：
1. **選定功能**：使用者每次選定**一個**欲撰寫的演算法函式（一次僅限開發一個，不得多個同時進行）。
2. **知識與原理說明**：AI 必須先詳細解釋該演算法涉及的**數學原理、核心觀念、影像處理工作流 (Workflow)** 等背景知識。
3. **架構規劃與確認**：
   * 等待使用者理解上述概念並回覆「**OK**」之後，AI 才能開始規劃該演算法的函式架構與實作邏輯。
   * 如果使用者提出詢問、調整需求或有疑義，AI 必須針對使用者需求進行解答與調整，**此時絕對不可以直接實作或提供完整程式碼**。
   * **特別協作路徑 (使用者自行編寫)**：若使用者回覆已經「手動/自己寫完成演算法程式」，在驗證其演算法邏輯正確後，AI 須分析專案的架構安排、合理性等資訊（可選擇提出架構可以如何進一步優化與更改的建議），隨後直接跳至 **5. 語法與編譯器驗證** 進行編譯測試並回報結果。若結果正確無誤，即可進入 **2. 測試階段**；若有編譯錯誤，則開始進行 Debug 或分析錯誤原因，並給予改進方向的建議。
4. **開始實作**：直到使用者明確回覆「**開始實作**」之後，AI 始可開始撰寫該演算法的 C++ 程式碼。
5. **語法與編譯器驗證**：
   * 程式碼撰寫完成後，AI 須立即進行專案編譯測試（MSBuild）。
   * **注意：** 此步驟的目的在於確認 C++ 語法正確性、編譯器與 LSP（語法分析器）是否有任何報錯或警告，**非測試演算法實際的影像處理功能**。

---

## 2. 測試階段 (Testing Stage)

此階段著重於單一演算法的整合功能測試與 UI 互動調整。

### 協作步驟：
1. **完整建置**：編譯整個方案項目（C++ DLL 與 C# 前端）。
2. **執行主程式**：運行產出的主程式執行檔 (`DIP.exe`)。
3. **提供測試指引**：針對使用者剛才選定並實作的演算法，AI 必須給予使用者具體的測試建議，包括：
   * **建議測試功能的步驟** (Step-by-step test guide)
   * **驗證目標** (Verification goals)
   * **預期結果** (Expected output / visual changes)
4. **回饋與 UI 微調與設計規範**：
   * **UI 版面規範**：凡是需要使用者手動調整參數、操作（而非直接生成新影像）的演算法功能，C# 前端視窗皆必須採用「上方為影像預覽 PictureBox，下方為參數調整操作 Panel」的整合式版面。
   * **即時預覽 (Real-time Preview)**：當使用者操作滑桿 (Slider) 或其他參數控制項時，必須能即時調用 C++ 演算法進行影像運算，並即時更新預覽的影像，提供使用者時時預覽的回饋體驗。
   * 根據使用者的實際測試反饋，AI 協助微調 C# 前端 UI（如控制項配置、文字標籤、滑桿數值範圍等）。
   * **重要規則：** 若測試中發現需要調整或修改「**演算法核心邏輯**」，必須立刻中斷測試，**退回到「1. 撰寫階段」**，從原理講解與架構重新開始，並等待使用者回覆 OK 才能繼續進行。
5. **彙整變更文件**：
   * 當使用者測試並回報輸出結果與預期相同（確認功能無誤）後，AI 須統整所有變更資訊，並彙整更新 `README.md` 及 `AGENTS.md` 檔案，確保專案文件即時反映最新狀態。

---

## 3. 最終實際部署環境測試 (Final Deployment Environment Testing)

當所有演算法函式皆完成實作後，才能進入此最終部署測試階段。**在所有功能未全部完成前，絕對不可以進入此階段。**

### 執行準則：
1. **確認完整實作**：確認 C++ 各功能模組中的所有 stub 函式皆已成功實作完畢。
2. **C# 載入確認**：確認 C# 專案能順利載入 `dip_proc.dll` 且沒有任何 P/Invoke 簽章不符之錯誤。
3. **無損編譯**：確認整個專案建置皆通過，達到 **`0 Error` (零錯誤)**。
4. **精簡部署環境驗證**：
   * 模擬最終實際部署環境，確保該環境**僅包含且必須包含**以下三個項目，不能有其他任何原始碼、專案檔或暫存快取：
     * `DIP.exe` (C# 執行檔)
     * `dip_proc.dll` (C++ 導出動態連結庫)
     * `Standard Images/` (標準測試影像資料夾)
5. **部署測試說明**：AI 必須提供使用者如何在這個乾淨的部署環境中，測試整個專案所有功能完整度的指南與預期表現。
6. **問題回退機制**：如果在部署環境測試中，使用者提出任何疑義或需要修改之處：
   * 若涉及**演算法**：立即退回 **「1. 撰寫階段」**。
   * 若涉及**視窗介面 (UI)**：立即退回 **「2. 測試階段」**。
   * 待修改完畢且經過使用者再次確認後，才能回到本階段。

---

## 4. 已開發演算法與功能模組追蹤 (Developed Modules)

| 演算法功能 | C++ 函式 | 狀態 | 說明 |
| :--- | :--- | :--- | :--- |
| **影像轉灰階** | `encode_gray` & `ConvertTo8bppGrayscale` | 已實作 | 採用 BT.601 標準公式。提供「24-bit 灰階顯示預覽」與「8-bit 灰階圖像轉換」雙子選單選項，後者寫入線性 256 色調色盤。 |
| **位元平面切片** | `bit_plane_slice` | 已實作 (Consolidated) | 整合式 MDI 預覽與控制項視窗。不符合 8-bit 格式之影像將自動轉換為 8-bit 灰階影像後執行。 |
| **統計直方圖數據** | `calculate_histogram` | 已實作 | 支援 3 通道 (BGR) 獨立統計（單通道時僅統計第一通道）。數據由 C# 前端直方圖側邊欄（Sidebar）讀取並動態分層渲染。 |
| **直方圖等化** | `histogram_equalization` | 已實作 | 支援單通道灰階與三通道 BGR 的獨立累計分佈函數 (CDF) 均衡化，對比度低之影像等化後可顯著增強細節。 |
| **亮度對比與 Gamma 調整** | `adjust_brightness_contrast` | 已實作 (Consolidated) | 整合式 MDI 預覽視窗 `BrightnessContrastGammaForm`，支援**線性模式**（Alpha 對比度 0.1~3.0、Beta 亮度 ±255，含等效 Gamma 動態算式推導）與**非線性 Gamma 冪律模式**（Gamma 0.1~10.0，滑桿 1.0 居中）。曲線預覽圖支援滑鼠點擊平移與拖曳互動操作。含「確定」輸出與「取消」、「重置預設值」按鈕。 |
| **影像旋轉** | `rotate_image` | 已實作 (Consolidated) | 整合式 MDI 預覽與參數控制視窗 `RotateImageForm`，支援插值模式（最近鄰、雙線性）與映射模式。輸出視窗 `MSForm` 整合背景調整欄位，支援「透明」、「黑色」、「白色」、「中間值」、「自訂」背景顏色，並支援「融入原始影像」功能與即時直方圖連動更新。已修正輸出視窗邊界裁切問題（重構為 1:1 PictureBox 尺寸與動態 Layout 居中，支援透明背景棋盤無縫融合與自訂選色歷史共享，以及非融入影像模式下背景顏色填滿整個預覽區之體驗）。 |
| **影像幾何縮放** | `scale_image` | 已實作 (Consolidated) | 支援最近鄰 (Nearest) 與雙線性 (Bilinear) 插值，引入分段線性滑桿映射將預設值 100% 定位於物理正中央。在 C++ 層實施嚴密座標 Clamp 與像素中心補償對齊，避免邊界溢位崩潰。 |
| **手動門檻二值化** | `manual_threshold` | 已實作 (Consolidated) | 整合式 MDI 預覽與控制視窗 `ManualThresholdForm`，支援 0~255 閥值即時預覽與直方圖同步連動。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。 |
| **大津法二值化** | `otsu_threshold` | 已實作 (Consolidated) | 自動最佳門檻二值化演算法，採用最大類間變異數公式計算最佳閾值。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。 |
| **霍夫直線偵測** | `detect_lines_hough` | 已實作 (Consolidated) | 整合式 MDI 預覽與控制項視窗 `HoughLineForm`，門檻上限提高至 290 且預設 150 置中。內建調色盤方格Click事件觸發自訂選色。針對大圖支援 Zoom 縮放預覽與 MouseMove 座標映射。支援 1bpp, 4bpp, 16bpp 等非位元組對齊點陣圖格式。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。 |
| **霍夫圓形偵測** | `detect_circles_hough` | 已實作 (Consolidated) | 整合式 MDI 預覽與控制項視窗 `HoughCircleForm`，支援半徑與門檻即時調整。C++ 使用梯度向量投影投票及 cos(10度)/sin(10度) 查表乘加優化，徹底移除三角函數解決卡頓。門檻預設為 50 修正失效問題。針對大圖支援 Zoom 縮放與座標映射。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。 |
| **Sobel 邊緣偵測** | `detect_sobel` | 已實作 (Consolidated) | 支援單通道灰階與三通道 BGR 自動灰階化運算。已修正預編譯標頭檔 `#include` 順序問題，移除了多餘的輸入至輸出全圖拷貝，提升大圖處理時的快取效能，並完美落實 3x3 卷積最外圈 1 像素的零邊界防護 (Zero Padding) 安全處理。 |
| **Canny 邊緣偵測** | `detect_canny` | 已實作 (Consolidated) | 整合式 MDI 預覽與控制項視窗 `CannyForm`，支援低、高雙門檻滑桿 (0~255) 即時調整與直方圖連動。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。 |
| **自訂與空間濾波器** | `convolution_filter` | 已實作 (Consolidated) | 支援自訂 3x3 與 5x5 濾波核心係數、除數與偏移量。非標準 8bpp 灰階影像將自動轉換為 8-bit 灰階影像後執行。其餘邊緣與平滑濾波器底層已重構為調用此通用卷積函式。 |
| **詳細技術知識文件** | `docs.md` | 已建立 | 詳盡記載 14 個已完成演算法之詳細數學原理、接口參數與 C++ 底層查找表 (LUT) 最佳化等實作細節，全以 ASCII 純文字公式表示。 |
| **功能與選單標題貼士** | `BindTooltip` | 已實作 | 滑鼠懸停 1.5 秒自動顯示各項核心演算法技術說明或選單分類簡介貼士，無超時自動關閉，滑鼠移開或點選時即刻隱藏。已補齊全部完成的功能（包括開啟檔案、空間濾波各子項、邊緣偵測各子項與霍夫偵測各子項），達成完整覆蓋。 |
| **C++ 核心模組化重構** | 5個模組C++原始檔 | 已實作 (Breaking Change) | 將原本巨大且單一的 `dip_proc.cpp` 拆分為：`color_ops.cpp` (色彩)、`intensity_ops.cpp` (明暗與直方圖)、`geometry_ops.cpp` (幾何轉換)、`threshold_ops.cpp` (閾值二值化) 及 `filter_edge_ops.cpp` (濾波邊緣與霍夫) 五個專一原始檔，藉由 `image_lib.h` 共用標頭匯出。 |
| **直方圖側邊欄永久顯示** | `DIPSample.cs` | 已實作 | 移除選單上的顯示直方圖切換按鈕，直方圖與統計面板在主程式載入後永久顯示於右側。 |
| **參數與演算法原理持久化** | 反射與屬性讀取 | 已實作 | 直方圖下方區域常駐顯示目前影像套用的演算法與參數。在即時調整預覽階段顯示 [即時預覽調整中] 及動態值，在確認輸出後顯示固定參數與中等詳細度的教育性演算法原理說明。 |

